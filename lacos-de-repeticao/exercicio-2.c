//Faça um programa que imprime os números de 20 a 29, separados por espaços, usando um comando de repetição.
#include <stdio.h>

int main(){
    int num=20;
    
    while(num<=29){
        printf("%d ", num);
        num++;
    }
}
